const lightTheme = {
  backgroundColor: "#eee",
  textColor: "#999",
};

const darkTheme = {
  backgroundColor: "#999",
  textColor: "#eee",
};

const Theme = {
  lightTheme,
  darkTheme,
};

export default Theme;
